return {
    -- Recommended: keep this set to `true`. Toggling a vehicle extra
    -- on/off internally repairs the vehicle (removes visual damage),
    -- so allowing the extras menu on a damaged car lets players use it
    -- as a free repair exploit.
    DisableExtrasIfDamaged = true,
}